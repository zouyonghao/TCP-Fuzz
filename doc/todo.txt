增加对断开连接、半开连接的测试

敏感度实验
    coverage(old/new) 随时间变化情况,及总数
    bug 数
    bug 出现时间
1. 传统coverage, 能否找到bug, 看if变化的coverage *
    1.1 统计new cov下old cov的变化
            - new_cov_normal_output
            - 目前在pc上运行tldk-20200924
            - zyh-laptop(32.305) 运行f-stack
        统计old cov下new cov的变化
            - old_cov_normal_output
2. 只fuzz包和只有syscall的对比
    2.1 old coverage + syscall ~= syzkaller
            - bugscan-laptop 运行 TLDK
            - virtual1 运行 F-stack
            - virtual2 运行 mTCP
        old coverage + packet ~= AFL
    2.2 old coverage + 单个包/syscall
        old coverage + 不考虑包和syscall关系, 随机

对比实验
1. 用afl测试
2. boofuzz
3. 文件系统测试方法,在syscall不变时,变包,交替进行 + 旧coverage *

扩展
1. tcp/ip: mtcp, fstack - done
2. icmp